{
  "name": "template-init-script-example",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "postinstall": "cd .init-script && npm install --silent --package-lock=false && node ."
  }
}
